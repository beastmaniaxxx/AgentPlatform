app:
  description: |-
    アップロード画像の公開URLをSerpAPI（REVERSE_IMAGE_ENGINE で選択したエンジン）で逆画像検索し、
    類似画像サムネイル（Markdown）と出典サイト一覧をローカルLLMで要約して返すワークフロー。
    対応エンジン: google_lens（既定）/ yandex_images / bing_reverse_image（Dify環境変数 REVERSE_IMAGE_ENGINE で切替）。
    インポート後にLLMノードのモデルをOllama接続済みのモデルに設定すること（docs/reverse-image-search-setup.md参照）。
    SERPAPI_KEY（Dify Secret環境変数）と REVERSE_IMAGE_ENGINE（Dify環境変数、既定: google_lens）をDify管理画面で設定すること。
  icon: "\U0001F50E"
  icon_background: '#EFF1F5'
  mode: advanced-chat
  name: reverse_image_search
  use_icon_as_answer_icon: false
dependencies: []
kind: app
version: 0.3.1
workflow:
  conversation_variables: []
  environment_variables: []
  features:
    file_upload:
      allowed_file_extensions: []
      allowed_file_types: []
      allowed_file_upload_methods:
      - local_file
      enabled: false
      number_limits: 3
    opening_statement: ''
    retriever_resource:
      enabled: false
    sensitive_word_avoidance:
      enabled: false
    speech_to_text:
      enabled: false
    suggested_questions: []
    suggested_questions_after_answer:
      enabled: false
    text_to_speech:
      enabled: false
      language: ''
      voice: ''
  graph:
    edges:
    - data:
        isInLoop: false
        sourceType: start
        targetType: http-request
      id: start-source-serpapi-target
      source: start
      sourceHandle: source
      target: serpapi_request
      targetHandle: target
      type: custom
      zIndex: 0
    - data:
        isInLoop: false
        sourceType: http-request
        targetType: code
      id: serpapi-source-normalize-target
      source: serpapi_request
      sourceHandle: source
      target: normalize_results
      targetHandle: target
      type: custom
      zIndex: 0
    - data:
        isInLoop: false
        sourceType: code
        targetType: if-else
      id: normalize-source-check-target
      source: normalize_results
      sourceHandle: source
      target: check_count
      targetHandle: target
      type: custom
      zIndex: 0
    - data:
        isInLoop: false
        sourceType: if-else
        targetType: answer
      id: check-true-no-results-target
      source: check_count
      sourceHandle: case_0_results
      target: answer_no_results
      targetHandle: target
      type: custom
      zIndex: 0
    - data:
        isInLoop: false
        sourceType: if-else
        targetType: llm
      id: check-false-summarize-target
      source: check_count
      sourceHandle: 'false'
      target: summarize
      targetHandle: target
      type: custom
      zIndex: 0
    - data:
        isInLoop: false
        sourceType: llm
        targetType: answer
      id: summarize-source-answer-target
      source: summarize
      sourceHandle: source
      target: answer_results
      targetHandle: target
      type: custom
      zIndex: 0
    nodes:
    - data:
        desc: ''
        selected: false
        title: Start
        type: start
        variables: []
      height: 54
      id: start
      position:
        x: 80
        y: 282
      positionAbsolute:
        x: 80
        y: 282
      sourcePosition: right
      targetPosition: left
      type: custom
      width: 244
    - data:
        authorization:
          config: null
          type: no-auth
        body:
          data: ''
          type: none
        desc: |-
          SerpAPIの逆画像検索APIを呼び出す。REVERSE_IMAGE_ENGINE で選択したエンジンを使用。
          url と image_url の両方に同じ公開URLを送信（エンジン別パラメータ名の差異を吸収）。
          ユーザー識別情報は付与しない。
        headers: ''
        method: GET
        params: ''
        selected: false
        timeout:
          connect: 60
          read: 60
          write: 20
        title: SerpAPI逆画像検索
        type: http-request
        url: 'https://serpapi.com/search?engine={{#env.REVERSE_IMAGE_ENGINE#}}&url={{#sys.query#}}&image_url={{#sys.query#}}&api_key={{#env.SERPAPI_KEY#}}'
        variables: []
      height: 126
      id: serpapi_request
      position:
        x: 384
        y: 282
      positionAbsolute:
        x: 384
        y: 282
      sourcePosition: right
      targetPosition: left
      type: custom
      width: 244
    - data:
        code: "import json\n\ndef main(body: str) -> dict:\n    try:\n        data\
          \ = json.loads(body)\n    except Exception:\n        return {\"count\":\
          \ 0, \"markdown\": \"\", \"items\": \"[]\"}\n    if data.get(\"error\"\
          ):\n        return {\"count\": 0, \"markdown\": \"\", \"items\": \"[]\"\
          }\n    raw_items = (\n        data.get(\"visual_matches\") or\n        data.get(\"\
          image_results\") or\n        data.get(\"inline_images\") or\n        []\n\
          \    )\n    items = []\n    markdown_parts = []\n    for r in raw_items:\n\
          \        thumbnail = r.get(\"thumbnail\") or \"\"\n        if not thumbnail:\n\
          \            continue\n        title = r.get(\"title\", \"\")\n        link\
          \ = r.get(\"link\") or r.get(\"url\") or \"\"\n        source = r.get(\"\
          source\") or r.get(\"domain\") or r.get(\"displayed_link\") or \"\"\n \
          \       items.append({\"title\": title, \"link\": link, \"source\": source,\
          \ \"thumbnail\": thumbnail})\n        markdown_parts.append(f\"![{title}]({thumbnail})\"\
          )\n        if len(items) >= 5:\n            break\n    count = len(items)\n\
          \    return {\n        \"count\": count,\n        \"markdown\": \"\\n\\\
          n\".join(markdown_parts),\n        \"items\": json.dumps(items, ensure_ascii=False)\n\
          \    }\n"
        code_language: python3
        desc: |-
          SerpAPIエンジン別の応答（visual_matches / image_results / inline_images）を
          共通形式（title / link / source / thumbnail）へ正規化し、上位5件のMarkdown画像と件数を算出する。
          SerpAPI error フィールド検出時は0件相当として扱う。
        outputs:
          count:
            children: null
            type: number
          items:
            children: null
            type: string
          markdown:
            children: null
            type: string
        selected: false
        title: 応答正規化
        type: code
        variables:
        - value_selector:
          - serpapi_request
          - body
          variable: body
      height: 126
      id: normalize_results
      position:
        x: 688
        y: 282
      positionAbsolute:
        x: 688
        y: 282
      sourcePosition: right
      targetPosition: left
      type: custom
      width: 244
    - data:
        cases:
        - case_id: case_0_results
          conditions:
          - comparison_operator: '='
            id: cond_count_zero
            value: '0'
            varType: number
            variable_selector:
            - normalize_results
            - count
          id: case_0_results
          logical_operator: and
        desc: 正規化後の類似画像件数が0かどうかを判定する。
        selected: false
        title: 結果件数チェック
        type: if-else
      height: 126
      id: check_count
      position:
        x: 992
        y: 282
      positionAbsolute:
        x: 992
        y: 282
      sourcePosition: right
      targetPosition: left
      type: custom
      width: 244
    - data:
        context:
          enabled: false
          variable_selector: []
        desc: |-
          類似画像と出典サイトを日本語で要約し、各画像に出典リンクを付与する。
          インポート後にモデルをOllama接続済みのモデルに設定すること。
        model:
          completion_params:
            temperature: 0.5
          mode: chat
          name: ''
          provider: ''
        prompt_template:
        - id: prompt_system
          role: system
          text: あなたは逆画像検索の結果を日本語でわかりやすく要約するアシスタントです。各類似画像について簡潔な説明を作成し、必ず出典を付けてください。
        - id: prompt_user
          role: user
          text: "以下の逆画像検索で見つかった類似画像の情報を日本語で要約してください。\n各画像について1〜2文の説明を付け、末尾に\
            \ `[出典: source](link)` 形式で出典を記載してください。\n\n類似画像一覧（JSON形式）:\n{{#normalize_results.items#}}"
        selected: false
        title: 要約生成
        type: llm
        variables: []
        vision:
          enabled: false
      height: 126
      id: summarize
      position:
        x: 1296
        y: 182
      positionAbsolute:
        x: 1296
        y: 182
      sourcePosition: right
      targetPosition: left
      type: custom
      width: 244
    - data:
        answer: "{{#normalize_results.markdown#}}\n\n---\n\n{{#summarize.text#}}"
        desc: ''
        selected: false
        title: 検索結果回答
        type: answer
        variables: []
      height: 105
      id: answer_results
      position:
        x: 1600
        y: 182
      positionAbsolute:
        x: 1600
        y: 182
      sourcePosition: right
      targetPosition: left
      type: custom
      width: 244
    - data:
        answer: |-
          類似画像が見つかりませんでした。

          別の画像や角度を変えた画像で再度お試しください。
        desc: ''
        selected: false
        title: 0件通知
        type: answer
        variables: []
      height: 105
      id: answer_no_results
      position:
        x: 1296
        y: 382
      positionAbsolute:
        x: 1296
        y: 382
      sourcePosition: right
      targetPosition: left
      type: custom
      width: 244
    viewport:
      x: 0
      y: 0
      zoom: 0.75
